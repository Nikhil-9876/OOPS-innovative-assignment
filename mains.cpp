#include <bits/stdc++.h>
using namespace std;
#define int long long
#define float double
#define gcd(a,b) __gcd(a,b)
#define sortv(a) sort(a.begin(),a.end())
#define all(a) a.begin(),a.end()
#define yes cout<<"YES"<<endl
#define print1(a) cout<<setprecision(30)<<a<<endl;
#define print2(a,b) cout<<setprecision(30)<<a<<" "<<setprecision(30)<<b<<endl;
#define no cout<<"NO"<<endl
#define sumv(a,x) accumulate(a.begin(),a.end(),x)
#define Sol ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
#define lcm(a,b) a*b/(gcd(a,b))
// __builtin_popcount(x),__builtin_clz(x)[leading zeros],__builtin_ctz(x)[trailing zeros]

void solve(){
    int n;
    cin>>n;
    vector<int> v(n);
    for(int i=0;i<n;i++) cin>>v[i];
}

int32_t main(){
    Sol
    // solve();
    int t;
    cin>>t;
    while(t--){
        solve();
    }
    
}